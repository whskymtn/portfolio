// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://whskymtn.github.io',
  base: '/portfolio', // Remove or set to '/' if your repository name is your-username.github.io
});